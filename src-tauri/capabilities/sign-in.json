{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sign-in",
  "description": "Capability for the sign-in window",
  "windows": ["sign-in"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-show",
    "core:webview:default"
  ]
}
